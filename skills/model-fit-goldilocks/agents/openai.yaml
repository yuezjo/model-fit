interface:
  display_name: "Model Fit · Goldilocks"
  short_description: "Conservative model advice; confirm before execution"
  default_prompt: "Use $model-fit-goldilocks to assess this task, recommend an available configuration, and stop before execution."
policy:
  allow_implicit_invocation: false
