interface:
  display_name: "Model Fit · Cheapskate"
  short_description: "Budget-minded model advice; confirm before execution"
  default_prompt: "Use $model-fit-cheapskate to assess this task, recommend an available configuration, and stop before execution."
policy:
  allow_implicit_invocation: false
