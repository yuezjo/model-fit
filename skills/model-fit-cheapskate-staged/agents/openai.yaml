interface:
  display_name: "Model Fit · Cheapskate · Staged"
  short_description: "Budget-minded model advice with stage-by-stage approval"
  default_prompt: "Use $model-fit-cheapskate-staged to assess this task, recommend an available configuration, and stop before execution."
policy:
  allow_implicit_invocation: false
